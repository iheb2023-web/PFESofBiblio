import 'package:flutter/material.dart';

class MesLivresPage extends StatelessWidget {
  const MesLivresPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Liste de mes livres',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
} 